#include "main.h"

/**
 * main - tests function that print if integer is positive or negative
 *
 * i: takes the integer to be tested
 *
 * Return: Returns 0
 */

int main(void)
{
	/*Initializing the variable*/
	int i;

	/*Statements*/
	i = 0; /*Assigning a value to the variable*/
	positive_or_negative(i); /*Calling the positive or negative function*/

	return (0);
}
