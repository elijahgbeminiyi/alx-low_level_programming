#include "main.h"

/**
 * swap_int - makes a swap
 * @a: first integer
 * @b: second integer
 * swap: defines the swap variable
 *
 * Return: function is void
 */

void swap_int(int *a, int *b)
{
	int swap;

	swap = *a
	*a = *b;
	*b = swap;
}
