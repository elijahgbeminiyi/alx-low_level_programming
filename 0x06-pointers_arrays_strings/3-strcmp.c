#include "main.h"
#include <string.h>

/**
 * _strcmp - This funtion compares two strings
 * @s1: first param
 * @s2: second param
 *
 * Return: returns the difference
 *
 */

int _strcmp(char *s1, char *s2)
{
	int diff;

	diff = strcmp(s1, s2);
	return (diff);
}
